---
slug: serverless-etl
status: "Series 01 · Serverless ETL"
featured: true
title: A Cost-Aware Serverless Pipeline for Batching Parquet into Apache Iceberg
description: "A daily batch of small Parquet files (plus the occasional huge one) lands in S3 Tables via a scheduled drain, size-based bin-packing, and one EMR Serverless job per group."
keywords: "AWS SAM · S3 · SQS · EventBridge Scheduler · Step Functions · EMR Serverless · Apache Iceberg · Lakehouse"
search: serverless etl s3 sqs eventbridge scheduler step functions emr serverless apache iceberg data lake lakehouse batching bin-pack ingestion
repoUrl: https://github.com/jabrennem/serverless-etl-s3-tables
outline:
  - Problem and solution
  - Demo and Results
  - Tradeoffs and next steps
---

import ArchitectureDiagram from './ArchitectureDiagram';
import ComputeDiagram from './ComputeDiagram';
import OrchestrationDiagram from './OrchestrationDiagram';
import StorageDiagram from './StorageDiagram';
import { Link } from 'react-router-dom';

export const Thumbnail = () => <ArchitectureDiagram thumbnail />;

I wanted to share an architecture for a data engineering problem I encountered for a real, 
slightly awkward workload in AWS. I start with the requirements and explain how they shaped the storage, 
orchestration, and compute layers. Then, I run an end-to-end test with a quick cost analysis at the end.
Finally, I finish with the current tradeoffs and list improvements I'd start on next if the volume grew.

[View the repository on GitHub →](https://github.com/jabrennem/serverless-etl-s3-tables)

<nav className="table-of-contents" aria-label="Table of contents">
  <p className="eyebrow">On this page</p>
  <ol>
    <li><Link to="#problem-and-solution">Problem and solution</Link></li>
    <li><Link to="#demo-and-results">Demo and Results</Link></li>
    <li><Link to="#tradeoffs-and-next-steps">Tradeoffs and next steps</Link></li>
  </ol>
</nav>

<ArchitectureDiagram />

<p className="part-kicker"><span>The problem</span><span aria-hidden="true">→</span><span>The solution</span></p>
<h2 id="problem-and-solution">Part 1: Problem and solution</h2>

### The problem

There is an external team that delivers a batch of small Parquet files to an S3 bucket every day. There are 
roughly 8 to 10 at a time with a few hundred to couple thousand records each. They usually arrive together,
but can also be delayed by hours. Each file is ingested into a specific Iceberg table in our catalog. Every 
once in a while, the same team also sends in the same batch much larger files with hundreds of millions of 
records that require some transformation logic.

I'm on a small team, so the design must require the least amount of resource management possible. When
something goes wrong I want to diagnose it quickly and retry the workflow so the data can be ingested again as 
quickly as possible. Cost must be minimal since the files are generally small. I don't want to pay for a 
heavyweight job unless it's for a file with millions of records.

That shapes four requirements. The pipeline should:
- consolidate a day's uploads into one run instead of reacting to each file
- scale to zero between batches
- spend compute in proportion to the data
- log enough history to trace a failure and safely re-run it.

<div className="design-principles" aria-label="Pipeline requirements">
  <div>
    <p className="design-principle-number">01</p>
    <h4>Batch-aware</h4>
    <p>Consolidate a window of uploads into one run, not one per file.</p>
  </div>
  <div>
    <p className="design-principle-number">02</p>
    <h4>Cost-effective</h4>
    <p>Scale to zero between batches; spend compute in proportion to the data.</p>
  </div>
  <div>
    <p className="design-principle-number">03</p>
    <h4>Observable</h4>
    <p>Make each execution and failure easy to trace and safe to re-run.</p>
  </div>
</div>
### The solution

I split the solution into three layers

- Storage
- Orchestration
- Compute

<div className="architecture-layer-heading">
  <span>01</span>
  <h4>Storage</h4>
</div>

This architecture separates storage for the incoming data feed and the Iceberg table data referenced by
downstream services and analytics teams. I store the source data in a general S3 bucket and use an S3 Table
Bucket for AWS-managed Iceberg tables. Incoming files from the external team land under the `feed/` prefix, 
and in my demo, each file resolves by name to one of two Iceberg tables (`test_data` and `customers`).

<StorageDiagram />

<div className="architecture-layer-heading">
  <span>02</span>
  <h4>Orchestration</h4>
</div>

The orchestration layer decides *when* to run and consolidates a window of uploads into a single 
execution for easy traceability. When the files are uploaded, it triggers an `S3 Object Created` 
event that is added to an SQS queue. The events wait in the queue until it's 
time to ingest. 

There is an EventBridge scheduler that invokes a Message Poller Lambda at set times daily to
check if there are messages in the queue. For example, if we poll at 9AM, we can launch 
the execution on the queued files to make sure they are ingested in the morning, and then
catch any delayed files with a 12PM check.

If the message poller finds files, it will assign a table name to the file based on its name 
and launch a single state machine execution with the full list of files from the queue. Step 
Functions provides a few tools that help with observability such as execution history, 
built-in error handling, and parallel task support.

<OrchestrationDiagram />

<div className="architecture-layer-heading">
  <span>03</span>
  <h4>Compute</h4>
</div>

The compute layer loads the Parquet files into Iceberg tables while trying to spend as little as 
possible. EMR Serverless is the expensive part, and each job is charged for start up 
time whether a job ingests a few KB or GB. Because of this, I group small Parquet
files together using a size threshold. The larger Parquet files that require 
special transformations use their own EMR job. That way, the blast radius is isolated if it fails
and doesn't affect the ingestion of other smaller files with simpler upload logic.

First, a Lambda validates the input and performs the file grouping logic. Every file over 
a configurable size threshold gets its own job. The state machine 
then runs one EMR job per group and a single job can write to several tables 
at once.

Jobs are idempotent. The Iceberg tables contain the original S3 location in a `source_file` 
column. Before appending data to a table, the job checks whether that file has already been ingested
or skips it. If a job fails, I can just rerun the state machine and do not risk data 
duplication. If any group fails, a Choice state fails the whole execution at the end which 
keeps the completed executions green and brings attention to the failed runs in the console.

As a bonus, each job drops a per-group stats JSON under `runs/<execution-id>/stats/` for downstream analytics teams
or dashboards to use, so nobody has to parse Spark logs.

<ComputeDiagram />

### How it's built

The whole thing is one AWS SAM application in a `template.yaml`. I'm not walking through the 
YAML here since the implementation can drift, and the diagrams above already show how the pieces fit. 
[The full source is on GitHub](https://github.com/jabrennem/serverless-etl-s3-tables) if you want to read it.

<aside className="next-steps-callout" id="whats-next">
  <p className="eyebrow">Up next</p>
  <p>With the problem and the design in place, let's run it end to end — upload a batch, watch it fan out, and check whether the grouping actually saved on EMR.</p>
</aside>

---

<br/>

<h2 id="demo-and-results">Part 2: Demo and Results</h2>

I'm going to test the architecture on a small demo. I deploy the stack, generate a batch of test files, and upload 
the whole folder at once. Then, I follow the single Step Functions execution as it fans out into one EMR job per
group, verify the rows landed in both Iceberg tables, and check whether grouping the small files actually saves
money on EMR.

<div className="architecture-layer-heading">
  <span>01</span>
  <h4>Deploy the stack</h4>
</div>

### Configure the deployment

Before deploying, I add the AWS Organization ID and the existing access logging bucket to the `parameter_overrides`
in `samconfig.yaml`:

```yaml
parameter_overrides:
  - PrincipalOrgId=o-xxxxxxxxxx      # your AWS Organization ID
  - LoggingBucketName=my-logging-bucket  # existing S3 bucket for access logs
```

### Build and deploy

With those values set, I specify an AWS profile and run the standard SAM build and deploy commands:

```bash
export AWS_PROFILE=profile_name
sam build
sam deploy
```

I run `sam build` to package the Lambda function and produce the transformed template used
for the deployment. This output is the first checkpoint that the application code and template 
built successfully.

<figure>
  <img
    src="/assets/media/serverless-etl/sam_build.png"
    alt="SAM build output"
    style={{width: '100%'}}
  />
</figure>

After I execute `sam deploy`, CloudFormation returns the stack outputs, including
the name of the data bucket used by the pipeline. These outputs confirm that the stack is ready for the
post-deployment setup.

<figure>
  <img
    src="/assets/media/serverless-etl/sam_deploy.png"
    alt="SAM deploy output"
    style={{width: '100%'}}
  />
</figure>

### Complete the post-deployment setup

The post-deployment script uploads the PySpark entrypoint to S3. In accounts where Lake Formation governs Data
Catalog access, it also applies the grants the EMR role needs on the catalog, database, and both tables
(`test_data` and `customers`).

```bash
./scripts/post-deploy.sh
```

The script is idempotent, so it is safe to run more than once.

<figure>
  <img
    src="/assets/media/serverless-etl/post_deploy.png"
    alt="Post-deploy script output"
    style={{width: '100%'}}
  />
</figure>

If the account uses IAM-only access control for the Data Catalog, only the PySpark upload is required and the Lake
Formation grant commands can be omitted.

<div className="architecture-layer-heading">
  <span>02</span>
  <h4>Run the pipeline</h4>
</div>

### Prepare the test batch

`scripts/generate_test_data.py` writes a folder of small Parquet files split across both tables, with a couple
of deliberately larger files mixed in so the size threshold has something to isolate. For demo purposes,
the size threshold is set at 10KB. This threshold can be a lot more nuanced and reflective of the data in
production.

```bash
python scripts/generate_test_data.py --out test-batch --count 10
```

### Trigger the workflow

`trigger.sh` uploads the whole folder to the `feed/` prefix.

```bash
./scripts/trigger.sh test-batch
```

<figure>
  <img
    src="/assets/media/serverless-etl/trigger_sh.png"
    alt="Running trigger.sh"
    style={{width: '100%'}}
  />
</figure>

In the `feed/` prefix the batch now sits as ten objects with different sizes. Most are a few KB, but
a couple are several times larger.

<figure>
  <img
    src="/assets/media/serverless-etl/feed-objects.png"
    alt="The feed/ prefix in S3 with ten uploaded parquet objects and their sizes"
    style={{width: '100%'}}
  />
</figure>

### Follow the execution

The Message Poller resolves each file to its table and hands the state machine a flat list. Since this 
is just a quick demo, the Lambda is checking the queue for messages every 2 minutes. This would 
obviously be different in production. 

The Map state then runs one EMR job per group, so the ten uploaded files become a single execution 
with 3 jobs. One for the small files packed together, and each large file on its own.

In this demo, I'm using the same Pyspark script logic for the big files, but more complicated transformations
could be implemented here. The transformations could increase processing time at production scale, so having
the transformations in parallel means differents job finish around the same time and the small files are
uploaded way before.

```json
[
  {
    "SourceFile": "feed/test-data-06-20260911T125144Z-10456.parquet",
    "TableName": "test_data",
    "Size": 12833
  },
  {
    "SourceFile": "feed/customers-01-20260911T125136Z-29403.parquet",
    "TableName": "customers",
    "Size": 4044
  },
  ...
]
```

As the workflow runs, successful states show green. The execution below fanned the batch out across its groups
and completed.

<figure>
  <img 
    src="/assets/media/serverless-etl/state-machine-succeeded.png"
    alt="Succeeded state machine execution"
    style={{maxWidth: '360px', width: '100%'}}
  />
</figure>

That one execution spawned 3 EMR jobs. The 2 large files each landed in their own single-file 
job (`grp0-1files`, `grp1-1files`), while all eight small files were packed into one shared job (`grp2-8files`).

<figure>
  <img
    src="/assets/media/serverless-etl/emr-jobs-grouped.png"
    alt="Three EMR Serverless job runs from one execution: grp0 and grp1 with one file each, grp2 with eight files"
    style={{width: '100%'}}
  />
</figure>

### Verify the Iceberg tables

Open the `test_data` table in the S3 Tables console and select **Preview** to inspect the rows the pipeline
wrote. Alongside the source columns, each row now carries the `ingest_datetime` stamped for its job run and the
`source_file` URI it came from.

<figure>
  <img 
    src="/assets/media/serverless-etl/table_bucket_preview_data.png" 
    alt="S3 Tables data preview"
    style={{width: '100%'}} 
  />
</figure>

The `customers` table is populated by the same run — same `ingest_datetime` as `test_data`, confirming the
batch fanned out to both tables from one execution.

<figure>
  <img
    src="/assets/media/serverless-etl/customers-preview.png"
    alt="S3 Tables preview of the customers table: customer_id, email, region, ingest_datetime, source_file"
    style={{width: '100%'}}
  />
</figure>

### Reruns are safe

To prove idempotency, I re-ran the same execution. The loader reads each file but skips the append
when that `source_file` is already in the table. Here's the driver log from the batched small-file job on the rerun:

```text
Batch of 8 file(s) into catalog <ACCOUNT_ID>:s3tablescatalog/serverless-etl-table-bucket
Loading .../feed/customers-01-...-29403.parquet into s3tablesbucket.default.customers
  read 165 records
  skip .../feed/customers-01-...-29403.parquet -- already loaded into 's3tablesbucket.default.customers'
Loading .../feed/test-data-04-...-29834.parquet into s3tablesbucket.default.test_data
  read 145 records
  skip .../feed/test-data-04-...-29834.parquet -- already loaded into 's3tablesbucket.default.test_data'
  ... (all 8 files skipped)
```

<div className="architecture-layer-heading">
  <span>03</span>
  <h4>Review the result</h4>
</div>

### Did grouping actually save money?

The batch contains 10 files: 8 small and 2 large. The 8 small files cost nearly the same to process
together as either large file did alone. This is because there of the per job floor. Reducing the number 
of jobs produced most of the savings. After each batch, the application auto-stops after two idle minutes 
to avoid ongoing charges between daily runs.


<table>
  <thead>
    <tr>
      <th>Workload</th>
      <th>EMR jobs</th>
      <th>Billed vCPU-hours</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>8 small files together (actual)</td>
      <td>1</td>
      <td>0.287</td>
    </tr>
    <tr>
      <td>Large file 1 (actual)</td>
      <td>1</td>
      <td>0.267</td>
    </tr>
    <tr>
      <td>Large file 2 (actual)</td>
      <td>1</td>
      <td>0.267</td>
    </tr>
    <tr>
      <td><strong>Grouped total (actual)</strong></td>
      <td><strong>3</strong></td>
      <td><strong>0.821</strong></td>
    </tr>
    <tr>
      <td><strong>Without grouping (estimated)</strong></td>
      <td><strong>10</strong></td>
      <td><strong>~2.67</strong></td>
    </tr>
    <tr>
      <td><strong>Reduction</strong></td>
      <td><strong>7 fewer</strong></td>
      <td><strong>69%</strong></td>
    </tr>
  </tbody>
</table>

---

<br/>

<h2 id="tradeoffs-and-next-steps">Part 3: Tradeoffs and next steps</h2>

Where I'd take it next:

- **Skip EMR entirely on a re-run.** 
   - Today the skip happens *inside* the job. A re-run still starts Spark to 
     find the files already loaded. Negligible at ~10 files/day, but a DynamoDB 
     "processed files" ledger checked in Lambda could filter previously uploaded
     files *before* EMR starts.
- **Protect persistent S3 data** from stack deletion or replacement.
- **Data-quality checks** before the append to mask or validate column values.

## Summary

A serverless pipeline that batches daily Parquet uploads into Apache Iceberg using a SQS buffer, 
scheduled polling, size-based file grouping, one EMR job per group. Built with AWS SAM. The
[code is on GitHub](https://github.com/jabrennem/serverless-etl-s3-tables).

