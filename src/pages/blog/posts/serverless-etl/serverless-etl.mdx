---
slug: serverless-etl
status: "Series 01 · Serverless ETL"
featured: true
title: Building an Event-Driven Data Lake on AWS with Apache Iceberg
description: "The initial architecture: prefix-scoped S3 events, EventBridge routing, Step Functions orchestration, and EMR Serverless execution."
keywords: "AWS SAM · S3 · EventBridge · Step Functions · EMR Serverless · Apache Iceberg · Lakehouse"
search: serverless etl s3 eventbridge step functions emr serverless apache iceberg data lake lakehouse athena ingestion
repoUrl: https://github.com/jabrennem/serverless-etl-s3-tables
outline:
  - Problem and solution
  - Implementation
  - Demo and Results
  - Tradeoffs and next steps
---

import ArchitectureDiagram from './ArchitectureDiagram';
import ComputeDiagram from './ComputeDiagram';
import OrchestrationDiagram from './OrchestrationDiagram';
import StorageDiagram from './StorageDiagram';
import { Link } from 'react-router-dom';

export const Thumbnail = () => <ArchitectureDiagram thumbnail />;

This project shows how I approach a data engineering architecture. It addresses a common challenge of 
reliably ingesting Parquet files from S3 into queryable Apache Iceberg tables without keeping compute 
resources running between workloads.

I start with the requirements, explain how they shaped the storage, orchestration, and compute layers, 
and then walk through the AWS SAM implementation. I finish with a cost analysis, current 
tradeoffs, and the improvements I would make next.

[View the repository on GitHub →](https://github.com/jabrennem/serverless-etl-s3-tables)

<nav className="table-of-contents" aria-label="Table of contents">
  <p className="eyebrow">On this page</p>
  <ol>
    <li><Link to="#problem-and-solution">Problem and solution</Link></li>
    <li><Link to="#implementation">Implementation</Link></li>
    <li><Link to="#demo-and-results">Demo and Results</Link></li>
    <li><Link to="#tradeoffs-and-next-steps">Tradeoffs and next steps</Link></li>
  </ol>
</nav>

<ArchitectureDiagram />

<p className="part-kicker"><span>The problem</span><span aria-hidden="true">→</span><span>The solution</span></p>
<h2 id="problem-and-solution">Part 1: Problem and solution</h2>

### The problem

Consider an upstream service that produces Parquet files and uploads them to S3. An analytics team needs 
the data ingested into managed Apache Iceberg tables in our data catalog in a timely manner. Uploads are infrequent 
and unpredictable, so the workload does not justify running resources.

That leaves three requirements for the pipeline. It should start processing when a file arrives, scale 
back down after the work finishes, and expose enough execution history to trace failures.

<div className="design-principles" aria-label="Pipeline requirements">
  <div>
    <p className="design-principle-number">01</p>
    <h4>Timely</h4>
    <p>Start processing as soon as a file arrives.</p>
  </div>
  <div>
    <p className="design-principle-number">02</p>
    <h4>Cost-effective</h4>
    <p>Avoid running compute between infrequent uploads.</p>
  </div>
  <div>
    <p className="design-principle-number">03</p>
    <h4>Observable</h4>
    <p>Make each execution and failure easy to trace.</p>
  </div>
</div>
### The solution

I split the solution into three layers so each concern can be designed and operated
independently:

- Storage
- Orchestration
- Compute

<div className="architecture-layer-heading">
  <span>01</span>
  <h4>Storage</h4>
</div>

This architecture separates storage locations for the incoming data feed and the Iceberg table data referenced 
by downstream services and analytics teams. I store the source data in a general S3 bucket and use S3 Table Buckets 
for the AWS managed Iceberg tables. This allows for a clear separation between raw data and the catalog table 
data. Incoming files are uploaded under the feed/ prefix, giving the pipeline a clear boundary for identifying 
new data to ingest.

<StorageDiagram />

<div className="architecture-layer-heading">
  <span>02</span>
  <h4>Orchestration</h4>
</div>

The orchestration layer is responsible for coordinating the ingestion process. The first step is to detect when new 
`feed/` objects are created. I configure the S3 bucket to send events to EventBridge. EventBridge rules can filter 
for events with the `feed/` prefix and then trigger a Step Functions state machine. Step Functions provides a visual 
execution history, built-in error handling, and support to run parallel tasks. Both EventBridge and Step Functions are
managed behind the scenes, so the entire orchestration layer is serverless and scales automatically.

<OrchestrationDiagram />

<div className="architecture-layer-heading">
  <span>03</span>
  <h4>Compute</h4>
</div>

The compute layer loads the incoming Parquet data into Iceberg. The current job reads each file as-is and writes 
its rows to the target table. I use Spark on EMR Serverless because it gives me control over the Spark configuration 
without keeping a cluster running between jobs. The Amazon S3 Tables integration exposes the table bucket through Glue's 
federated `s3tablescatalog`, allowing Spark to read from and write to the Iceberg tables.

There is an opportunity for collecting runtime statistics that downstream services can use for notifications or 
anomaly detection. The job writes these statistics as JSON under `runs/<execution-id>/stats/` in the source S3 
bucket. Using the execution ID connects the output to a specific Step Functions execution.

Before starting EMR Serverless, I use a Lambda function to validate the EventBridge payload and map each source file to an 
Iceberg table. This allows malformed or unmapped input to fail quickly and cheaply while keeping the Spark job focused on 
loading the data.

<ComputeDiagram />

<aside className="next-steps-callout" id="whats-next">
  <p className="eyebrow">Up next</p>
  <p>Now that we have a clear understanding of the problem and the solution, we can dive into the source code and see how the architecture is implemented.</p>
</aside>

---

<br/>

<p className="part-kicker"><span>Implementation</span></p>
<h2 id="implementation">Part 2: Implementation</h2>

The entire stack is defined in a single `template.yaml` using AWS SAM. This section walks
through what goes into each layer of the architecture that we mapped out in part 1. Security is applied
within each layer rather than treated as a separate component. The full source code is available in the
[GitHub repository](https://github.com/jabrennem/serverless-etl-s3-tables).

<div className="architecture-layer-heading">
  <span>01</span>
  <h4>Storage</h4>
</div>

### Encrypting the data bucket

A customer-managed KMS key encrypts every object in the data bucket. This is very important when incoming data is
sensitive like PII or health information. Auto-rotation is enabled and the bucket uses S3 Bucket Key to reduce 
per-request KMS calls.

```yaml
EncryptionKey:
  Type: AWS::KMS::Key
  Properties:
    Description: KMS key for S3 Bucket
    EnableKeyRotation: true
```

### Organizing the data bucket

In this design, we are using the 'DataBucket' for a few different purposes:
  - `feed/` the source data feed
  - `emr/` the runtime PySpark script
  - `runs/` the output for runtime statistics

The EventBridge notifications are enabled so our event rule defined later on can react to `feed/` uploads.
We also apply very common production practices, such as enabling versioning, public access blocks, access logging, 
and lifecycle rules. 

```yaml
DataBucket:
  Type: AWS::S3::Bucket
  Properties:
    NotificationConfiguration:
      EventBridgeConfiguration:
        EventBridgeEnabled: true
    BucketEncryption:
      ServerSideEncryptionConfiguration:
        - ServerSideEncryptionByDefault:
            SSEAlgorithm: aws:kms
            KMSMasterKeyID: !GetAtt EncryptionKey.Arn
          BucketKeyEnabled: true
    VersioningConfiguration:
      Status: Enabled
    PublicAccessBlockConfiguration:
      BlockPublicAcls: true
      BlockPublicPolicy: true
      IgnorePublicAcls: true
      RestrictPublicBuckets: true
    LoggingConfiguration:
      DestinationBucketName: !Ref LoggingBucketName
      LogFilePrefix: !Sub "${AWS::StackName}/"
    LifecycleConfiguration:
      Rules:
        - Id: ExpireNonCurrentVersions
          Status: Enabled
          NoncurrentVersionExpiration:
            NoncurrentDays: 30
```

To further control access to the bucket, we apply a bucket policy that rejects any request 
that does not use HTTPS and blocks access outside of the organization.

```yaml
BucketPolicy:
  Type: AWS::S3::BucketPolicy
  Properties:
    Bucket: !Ref DataBucket
    PolicyDocument:
      Version: "2012-10-17"
      Statement:
        - Sid: DenyInsecureTransport
          Effect: Deny
          Principal: "*"
          Action: "s3:*"
          Resource:
            - !GetAtt DataBucket.Arn
            - !Sub "${DataBucket.Arn}/*"
          Condition:
            Bool:
              aws:SecureTransport: "false"
        - Sid: AllowOrganizationAccess
          Effect: Allow
          Principal:
            AWS: "*"
          Action:
            - s3:GetObject
            - s3:PutObject
            - s3:DeleteObject
            - s3:ListBucket
            - s3:GetBucketLocation
          Resource:
            - !GetAtt DataBucket.Arn
            - !Sub "${DataBucket.Arn}/*"
          Condition:
            StringEquals:
              aws:PrincipalOrgID: !Ref PrincipalOrgId
```

### Defining the Iceberg destination

Three CloudFormation resources define the S3 Table Bucket destination. S3 Table Buckets use
a 3 layer hierarchy for accessing the Iceberg tables.

  - Table Bucket
  - The Namespace
  - The Table

I chose to declare the table in the template to make this service simple. It also means it exists before 
the first pipeline run, so there is no "create table if notexists" logic in Spark.

```yaml
IcebergTableBucket:
  Type: AWS::S3Tables::TableBucket
  Properties:
    TableBucketName: !Ref TableBucketName

IcebergDefaultNamespace:
  Type: AWS::S3Tables::Namespace
  Properties:
    TableBucketARN: !GetAtt IcebergTableBucket.TableBucketARN
    Namespace: default

# test table for this service with a simple schema
IcebergTestDataTable:
  Type: AWS::S3Tables::Table
  DependsOn: IcebergDefaultNamespace
  Properties:
    TableBucketARN: !GetAtt IcebergTableBucket.TableBucketARN
    Namespace: default
    TableName: test_data
    OpenTableFormat: ICEBERG
    IcebergMetadata:
      IcebergSchema:
        SchemaFieldList:
          - Name: id
            Type: long
            Required: true
          - Name: name
            Type: string
            Required: false
          - Name: amount
            Type: double
            Required: false
```

<div className="architecture-layer-heading">
  <span>02</span>
  <h4>Orchestration</h4>
</div>

### Coordinating independent loads

The state machine language comes with a few helpful tools that reduces our need for futher lambdas. We use 
JSONata (not JSONPath) which allows for more complex data transformations in our state definitions and the
`.sync` integration pattern so Step Functions polls EMR until completion.

#### ASL highlights

There is a Map state which iterates over resolved table mappings, so each input file gets an
independent EMR → stats → emit pipeline. Its input is simply:

```json
"Items": "{% $states.input.SourceFileTableNameMapping %}"
```

Each item submits `load_data.py` synchronously, passing the source object, catalog ID,
namespace, target table, and an execution-scoped stats location. Failed items route to
`Fail Item`, so one table's failure does not prevent the remaining items from running.

The submit configuration is kept separate from the Map logic defined as the `SparkSubmitParameters` string.

```bash
--conf spark.jars=/usr/share/aws/iceberg/lib/iceberg-spark3-runtime.jar
--conf spark.sql.extensions=org.apache.iceberg.spark.extensions.IcebergSparkSessionExtensions
--conf spark.sql.catalog.s3tablesbucket=org.apache.iceberg.spark.SparkCatalog
--conf spark.sql.catalog.s3tablesbucket.type=rest
--conf spark.sql.catalog.s3tablesbucket.uri=https://glue.${AWS::Region}.amazonaws.com/iceberg
--conf spark.sql.catalog.s3tablesbucket.warehouse=${GlueCatalogId}
--conf spark.sql.catalog.s3tablesbucket.rest.sigv4-enabled=true
--conf spark.sql.catalog.s3tablesbucket.rest.signing-name=glue
--conf spark.sql.catalog.s3tablesbucket.rest.signing-region=${AWS::Region}
```

Together, these configure Spark's `s3tablesbucket` catalog to use the AWS Glue Iceberg
REST endpoint, sign requests with SigV4, and enable Iceberg's Spark extensions.

After the Map completes, a Choice state counts failed items — if any table has
`status: "failed"`, the execution fails. This makes partial success visible in the
Step Functions console.

```json
"Check Table Results": {
  "Type": "Choice",
  "Choices": [{
    "Condition": "{% $count($states.input[status = 'failed']) > 0 %}",
    "Next": "Fail"
  }],
  "Default": "Succeed"
}
```

### Starting the workflow from S3

An EventBridge rule filters for `Object Created` events under the `feed/` prefix
and invokes the state machine via an `InputTransformer` that extracts the S3 key
into the expected shape that will be sent to the `ValidateInputFunction`.

```yaml
FeedUploadRule:
  Type: AWS::Events::Rule
  Properties:
    EventPattern:
      source:
        - aws.s3
      detail-type:
        - Object Created
      detail:
        bucket:
          name:
            - !Ref DataBucket
        object:
          key:
            - prefix: "feed/"
    Targets:
      - Id: LoadDataStateMachine
        Arn: !GetAtt StateMachine.Arn
        RoleArn: !GetAtt EventBridgeStateMachineRole.Arn
        InputTransformer:
          InputPathsMap:
            key: $.detail.object.key
          InputTemplate: |
            {
              "SourceFileTableNameMapping": [{"SourceFile": "<key>"}],
              "Event": {"source": "aws.s3", "detail-type": "Object Created", "key": "<key>"}
            }
```

The `EventBridgeStateMachineRole` has a single permission — `states:StartExecution`
scoped to the state machine ARN. This keeps the event trigger limited to starting this workflow.

<div className="architecture-layer-heading">
  <span>03</span>
  <h4>Compute</h4>
</div>

### Failing before EMR starts

A sub-second Lambda that fails the execution fast and cheap before running EMR.
It validates the EventBridge payload against a JSON Schema and resolves which Iceberg
table to target using regex patterns from `file_mapping.json`.

```yaml
ValidateInputFunction:
  Type: AWS::Serverless::Function
  Properties:
    Handler: validate_input.handler
    CodeUri: lambda/validate_input/
```

#### Handler highlights

This is the main handler behind the Lambda.

```python
@logger.inject_lambda_context
def handler(event, context):
    validate(event=event, schema=INPUT_SCHEMA)

    for mapping in event["SourceFileTableNameMapping"]:
        if "TableName" not in mapping:
            table_name = resolve_table_name(
                mapping["SourceFile"], FILE_PATTERN_MAPPINGS
            )
            mapping["TableName"] = table_name

    return event
```

Table resolution matches the S3 key against regex patterns sorted by specificity
(longest first). Adding a new ingestion target requires only a new entry in
`file_mapping.json` and a corresponding table resource in the SAM template.

```json
{
  "mappings": [
    {"tableName": "test_data", "filePattern": ".*/test-data\\.parquet$"}
  ]
}
```

### Scaling compute to zero

A Spark application on EMR release 7.8.0 (ARM64). Auto-start and auto-stop with a
2-minute idle timeout means zero cost when the pipeline isn't running.

```yaml
EmrApp:
  Type: AWS::EMRServerless::Application
  Properties:
    ReleaseLabel: emr-7.8.0
    Architecture: ARM64
    Type: Spark
    AutoStartConfiguration:
      Enabled: true
    AutoStopConfiguration:
      Enabled: true
      IdleTimeoutMinutes: 2
```

The validation Lambda and EMR jobs use separate execution roles so each can be limited to the resources it needs.
When Lake Formation governs Data Catalog access, the EMR role also needs the grants shown in Part 3.

#### Loading Parquet into Iceberg

The `load_data.py` script is intentionally minimal: read Parquet, write to Iceberg
via `INSERT OVERWRITE`, and emit a stats JSON for the orchestrator. Catalog configuration
lives entirely in `SparkSubmitParameters`, keeping the script portable.

```python
def main(input_path, catalog_id, namespace, table_name, stats_s3_uri=""):
    iceberg_table = f"s3tablesbucket.{namespace}.{table_name}"

    spark = (
        SparkSession.builder
        .appName(f"S3TableBucket-Import-{table_name}")
        .getOrCreate()
    )

    df = spark.read.parquet(input_path)
    count = df.count()

    df.createOrReplaceTempView("source_data")
    spark.sql(f"INSERT OVERWRITE {iceberg_table} SELECT * FROM source_data")

    if stats_s3_uri:
        _write_stats_file(spark, stats_s3_uri, input_path, catalog_id,
                          namespace, table_name, count)
    spark.stop()
    return 0
```

The stats file written to `runs/<execution-id>/stats/<table>.json` is read back by
the state machine so structured metadata flows downstream without parsing Spark logs.

---

<br/>

<h2 id="demo-and-results">Part 3: Demo and Results</h2>

<div className="architecture-layer-heading">
  <span>01</span>
  <h4>Commands</h4>
</div>

The deployment process follows a typical SAM lifecycle. Before deploying, open `samconfig.yaml` and fill in
the `parameter_overrides` values for your environment:

```yaml
parameter_overrides:
  - PrincipalOrgId=o-xxxxxxxxxx      # your AWS Organization ID
  - LoggingBucketName=my-logging-bucket  # existing S3 bucket for access logs
```

Once those are set, specify a profile and run sam build/deploy. A 
`post-deploy.sh` script handles uploading the PySpark entrypoint to S3 and applies Lake Formation grants 
for accounts that use Lake Formation to govern Data Catalog access.

```bash
export AWS_PROFILE=profile_name
sam build
sam deploy
./scripts/post-deploy.sh
```

`sam build` packages the Lambda function and produces the transformed template used
for deployment.

<figure>
  <img src="/assets/media/serverless-etl/sam_build.png" alt="Terminal output showing a successful SAM build for the ValidateInputFunction Lambda." style={{width: '100%'}} />
</figure>

After `sam deploy` completes, CloudFormation returns the stack outputs, including
the name of the data bucket used by the pipeline.

<figure>
  <img src="/assets/media/serverless-etl/sam_deploy.png" alt="Terminal output showing a successful SAM deployment and the deployed stack's DataBucketName output." style={{width: '100%'}} />
</figure>

The post-deploy script is idempotent — safe to re-run on every deploy:

```bash
# Upload PySpark entrypoint
aws s3 cp emr/load_data.py "s3://${DATA_BUCKET}/emr/load_data.py"

# Required only when Lake Formation permissions govern the catalog:
# apply grants (catalog → database → table)
aws lakeformation grant-permissions \
  --principal "DataLakePrincipalIdentifier=${EMR_ROLE_ARN}" \
  --resource '{"Catalog":{"Id":"<catalog-id>"}}' \
  --permissions '["CREATE_DATABASE", "DESCRIBE"]'
```

<figure>
  <img src="/assets/media/serverless-etl/post_deploy.png" alt="Terminal output showing a successful post-deploy script run — uploading the PySpark script and applying Lake Formation grants." style={{width: '100%'}} />
</figure>

### Why the Lake Formation grants matter

Lake Formation is optional here. By default, Lake Formation uses **Use only IAM access
control** for new Data Catalog resources, so IAM policies control access. In the account
used for this deployment, Lake Formation permissions governed the catalog instead. In
that mode, IAM permissions alone are not enough for an EMR job to work with the catalog:
the EMR role must also be explicitly granted access to the S3 Tables catalog, its
`default` namespace, and the tables inside it.

If your account retains the IAM-only default, the PySpark upload is the only
post-deployment step needed; omit the Lake Formation grant commands.

The script applies the least privileges the job needs at each level:

- **Catalog** — `CREATE_DATABASE` and `DESCRIBE`, so the job can discover the catalog
  and create a database if needed.
- **Namespace** — `DESCRIBE`, `ALTER`, and `CREATE_TABLE`, so it can inspect and manage
  the `default` namespace.
- **Tables** — `SELECT`, `INSERT`, `DESCRIBE`, `ALTER`, and `DROP`, so it can read,
  write, and maintain the Iceberg tables created by the pipeline.

These grants are applied after deployment because the script derives both the deployed
EMR role and the account-specific S3 Tables catalog ID. Re-running it is safe: grants
that already exist are ignored.

<div className="architecture-layer-heading">
  <span>02</span>
  <h4>Live Test</h4>
</div>

### Test data

The test file is a minimal 3-row Parquet file (1.3 KB) with the same schema as the
Iceberg table declared in the SAM template:

<table>
  <thead>
    <tr><th>id</th><th>name</th><th>amount</th></tr>
  </thead>
  <tbody>
    <tr><td>1</td><td>Alice</td><td>100.50</td></tr>
    <tr><td>2</td><td>Bob</td><td>200.75</td></tr>
    <tr><td>3</td><td>Charlie</td><td>350.00</td></tr>
  </tbody>
</table>

If you want to generate your own test file, there's a script in the repo at
`scripts/generate_test_data.py`:

```python
import pyarrow as pa
import pyarrow.parquet as pq

table = pa.table({
    "id": pa.array([1, 2, 3], type=pa.int64()),
    "name": pa.array(["Alice", "Bob", "Charlie"], type=pa.string()),
    "amount": pa.array([100.50, 200.75, 350.00], type=pa.float64()),
})

pq.write_table(table, "test-data.parquet")
```

### Trigger command

The `trigger.sh` script uploads a Parquet file to the `feed/` prefix, which fires
the EventBridge rule and starts the state machine.

```bash
./scripts/trigger.sh test-data.parquet
# → Uploading test-data.parquet to s3://<bucket>/feed/...
# → Pipeline triggered. Monitor execution in the Step Functions console.
```

<figure>
  <img src="/assets/media/serverless-etl/trigger_sh.png" alt="Terminal output showing trigger.sh uploading a parquet file to the feed/ prefix and confirming the pipeline was triggered." style={{width: '100%'}} />
</figure>

Below you can see the uploaded parquet file in S3 under the `feed/` prefix.

<figure>
  <img src="/assets/media/serverless-etl/s3-test-data-object.png" alt="S3 console showing the uploaded test-data.parquet file in the feed/ prefix of the data bucket." style={{maxWidth: '720px', width: '100%'}} />
</figure>

### Eventbridge and State Machine Input

The EventBridge `InputTransformer` constructs this payload from the S3 event:

```json
{
  "SourceFileTableNameMapping": [
    {"SourceFile": "feed/test-data.parquet"}
  ],
  "Event": {
    "source": "aws.s3",
    "detail-type": "Object Created",
    "key": "feed/test-data.parquet"
  }
}
```

As the workflow runs through each step, you can see successful states as green. The
image below is what a successful execution looks like.

<figure>
  <img src="/assets/media/serverless-etl/state-machine-succeeded.png" alt="Step Functions Graph view showing a succeeded execution — Validate Input, Load Tables map with Load Data, Get Stats Object, Emit Job Complete, Item Succeeded, then Check Table Results routing to Succeed." style={{maxWidth: '480px', width: '100%'}} />
</figure>

### Verify the loaded data

Open the `test_data` table in the S3 Tables console and select **Preview** to inspect
the rows written by the pipeline.

<figure>
  <img src="/assets/media/serverless-etl/table_bucket_preview_button.png" alt="S3 Tables console for the test_data table with the Preview button highlighted." style={{width: '100%'}} />
</figure>

The preview confirms that all three records from the test file landed in the Iceberg
table.

<figure>
  <img src="/assets/media/serverless-etl/table_bucket_preview_data.png" alt="S3 Tables preview showing three test_data rows: Alice with 100.5, Bob with 200.75, and Charlie with 350." style={{width: '100%'}} />
</figure>

<div className="architecture-layer-heading">
  <span>03</span>
  <h4>Cost Breakdown</h4>
</div>

The pipeline costs effectively nothing at rest. Here's the actual cost of one test
execution processing a 1.3 KB Parquet file through 8 state transitions in 2 min 31 s
total state machine duration:

<table>
  <thead>
    <tr>
      <th>Service</th>
      <th>Usage</th>
      <th>Cost</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>Step Functions</strong></td>
      <td>8 state transitions × $0.000025</td>
      <td>$0.000200</td>
    </tr>
    <tr>
      <td><strong>Lambda</strong> (ARM64, 128 MB)</td>
      <td>80 ms duration, 1 invocation</td>
      <td>$0.000001</td>
    </tr>
    <tr>
      <td><strong>EMR Serverless</strong> (ARM64)</td>
      <td>~2 min 24 s total, 0.267 vCPU-hours / 1.067 GB-hours billed</td>
      <td>$0.016</td>
    </tr>
    <tr>
      <td><strong>S3</strong></td>
      <td>2 PUTs + 3 GETs + ~4 KB stored</td>
      <td>$0.000011</td>
    </tr>
    <tr>
      <td><strong>EventBridge</strong></td>
      <td>2 events (S3 trigger + custom)</td>
      <td>$0.000001</td>
    </tr>
    <tr>
      <td><strong>Total</strong></td>
      <td></td>
      <td><strong>≤ $0.016</strong></td>
    </tr>
  </tbody>
</table>

The cost is dominated almost entirely by EMR Serverless compute time. Everything else
rounds to effectively zero. The 2-minute auto-stop idle timeout means the EMR
application shuts down immediately after the job completes, so there's no ongoing cost
between runs.

For context, Step Functions provides 4,000 free state transitions per month and Lambda
includes 1M requests + 400,000 GB-seconds free — so for light development and testing,
those services are literally $0.

### Resource Tags

The stack is tagged with an application identifier, which means all resources created by
CloudFormation inherit the tag. This integrates with AWS Cost Explorer, AWS Budgets, and
Cost Allocation Tags to track spend per pipeline — useful when you're running multiple
workloads in the same account and want to attribute costs back to individual services or
teams.

---

<br/>

<h2 id="tradeoffs-and-next-steps">Part 4: Tradeoffs and next steps</h2>

This pipeline is deliberately simple - one file in, one table out. Moving toward production
means adding fail-safety to the pipeline.

The most immediate gap is **durability**. The DataBucket has no `DeletionPolicy`, so tearing
down the stack removes it. Setting `Retain` is a one-line fix but easy to forget.

Next is **throughput**. Right now each file triggers its own execution. That's fine at low
volume, but at dozens of files per minute the per-execution overhead adds up. Fronting the
EventBridge rule with an SQS queue and a small batching window lets multiple files share a
single state machine run, cutting Step Functions transitions and EMR cold starts.

Finally, there's the data schema itself. The table schema is defined in the SAM template,
which means schema changes go through version control and deploy — intentional, not accidental.
A lightweight data quality check before the write in EMR (validating column types and
rejecting unexpected schemas) would make the pipeline not just observable, but trustworthy.

### Summary

For data engineering teams, this is a practical way to turn an uploaded Parquet file
into a queryable Iceberg table without keeping infrastructure running between workloads.
New data is picked up as soon as it lands, each ingestion has a visible execution
history, and Spark compute spins up only when there is work to do. The result is a
pipeline that is timely, cost-effective, and observable, with clear places to extend it
as data volume, table count, and operational requirements grow.
